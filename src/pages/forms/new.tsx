// placeholder for /forms/new page
import React from "react";
import { NextPage } from "next";
import { useRouter } from "next/router";

const FormsNew: NextPage = () => {
  const router = useRouter();

  return (
    <div>
      <h1>forms new page</h1>
    </div>
  );
};

export default FormsNew;